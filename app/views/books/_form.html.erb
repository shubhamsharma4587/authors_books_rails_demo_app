<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%#= form.label :book_id %>
  <%#= form.number_field :book_id %>
  </div>-->

  <div class="field">
    <%= form.label :book_title %>
    <%= form.text_field :book_title, {required: true} %>
  </div>

  <div class="field">
    <%= form.label :authors %>
    <%= form.select :author_ids, Author.pluck(:author_id, :first_name, :last_name).map { |author_id, first_name, last_name| ["#{first_name} #{last_name}", author_id] }, {prompt: "Select"}, {required: true, multiple: true} %>
    <%#= form.text_field :author_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
